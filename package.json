{
  "name": "ar-photography",
  "version": "1.0.0",
  "description": "Product photography viewed in AR",
  "author": "linnea.watson",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "deploy": "npm run generate && push-dir --dir=dist --branch=gh-pages --cleanup --overwrite-local --force"
  },
  "dependencies": {
    "@google/model-viewer": "^0.5.0",
    "nuxt": "^2.15.8"
  },
  "devDependencies": {
    "nodemon": "^1.18.9"
  }
}
